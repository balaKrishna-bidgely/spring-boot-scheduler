name: Run Spring Boot Application

on:
  workflow_dispatch:  # Manual trigger
    inputs:
      duration_minutes:
        description: 'Duration to run the app (in minutes)'
        required: true
        default: '60'
  # schedule:
  #   - cron: '0 */6 * * *'  # Optional: Run every 6 hours

jobs:
  run-application:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: 1234
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Set environment variables
        run: |
          # Export environment variables to GITHUB_ENV so they persist across steps
          echo "SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/postgres" >> $GITHUB_ENV
          echo "SPRING_DATASOURCE_USERNAME=postgres" >> $GITHUB_ENV
          echo "SPRING_DATASOURCE_PASSWORD=1234" >> $GITHUB_ENV
          echo "AWS_REGION=${{ secrets.AWS_REGION || 'us-west-2' }}" >> $GITHUB_ENV
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV
          echo "SPRING_MAIL_USERNAME=${{ secrets.GMAIL_USERNAME }}" >> $GITHUB_ENV
          echo "SPRING_MAIL_PASSWORD=${{ secrets.GMAIL_APP_PASSWORD }}" >> $GITHUB_ENV

          echo "✅ Environment variables configured"

      - name: Start Spring Boot Application
        run: |
          echo "Starting Spring Boot application..."
          echo "Logs will be written to app.log"
          echo ""
          echo "🔧 Configuration:"
          echo "  - Database: jdbc:postgresql://localhost:5432/postgres"
          echo "  - AWS Region: ${AWS_REGION}"
          echo "  - AWS Access Key: ${AWS_ACCESS_KEY_ID:0:10}..." # Show first 10 chars only
          echo "  - Email Username: ${SPRING_MAIL_USERNAME}"
          echo ""

          # Start the application with explicit environment variables
          nohup env \
            SPRING_DATASOURCE_URL="${SPRING_DATASOURCE_URL}" \
            SPRING_DATASOURCE_USERNAME="${SPRING_DATASOURCE_USERNAME}" \
            SPRING_DATASOURCE_PASSWORD="${SPRING_DATASOURCE_PASSWORD}" \
            AWS_REGION="${AWS_REGION}" \
            AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY_ID}" \
            AWS_SECRET_ACCESS_KEY="${AWS_SECRET_ACCESS_KEY}" \
            SPRING_MAIL_USERNAME="${SPRING_MAIL_USERNAME}" \
            SPRING_MAIL_PASSWORD="${SPRING_MAIL_PASSWORD}" \
            java -jar target/*.jar > app.log 2>&1 &

          APP_PID=$!
          echo "APP_PID=$APP_PID" >> $GITHUB_ENV
          echo "Application started with PID: $APP_PID"

          # Wait a moment for the log file to be created
          sleep 3

          # Show initial logs
          echo ""
          echo "=========================================="
          echo "📋 Initial Application Output:"
          echo "=========================================="
          if [ -f app.log ]; then
            head -n 20 app.log
          else
            echo "⚠️  Log file not created yet"
          fi
          echo "=========================================="

      - name: Wait for application to start
        run: |
          echo "Waiting for application to be ready..."
          echo ""

          # Check if app process is running
          if ! ps -p $APP_PID > /dev/null; then
            echo "❌ ERROR: Application process died immediately!"
            echo ""
            echo "Application logs:"
            cat app.log 2>/dev/null || echo "No log file found"
            exit 1
          fi

          for i in {1..30}; do
            # Check if process is still alive
            if ! ps -p $APP_PID > /dev/null; then
              echo ""
              echo "❌ ERROR: Application process died during startup!"
              echo ""
              echo "=========================================="
              echo "📋 FULL APPLICATION LOG:"
              echo "=========================================="
              cat app.log 2>/dev/null || echo "No log file found"
              echo "=========================================="
              exit 1
            fi

            # Try to connect to the application
            if curl -f http://localhost:8080/actuator/health 2>/dev/null || curl -f http://localhost:8080 2>/dev/null; then
              echo ""
              echo "Application is ready!"
              echo ""
              echo "=========================================="
              echo "📋 STARTUP LOGS (Last 50 lines):"
              echo "=========================================="
              tail -n 50 app.log 2>/dev/null || echo "No log file found"
              echo "=========================================="
              break
            fi

            echo "Attempt $i/30: Application not ready yet, waiting..."

            # Show logs at intervals
            if [ $i -eq 10 ] || [ $i -eq 20 ]; then
              echo ""
              echo "=========================================="
              echo "📋 Current application logs (last 30 lines):"
              echo "=========================================="
              if [ -f app.log ]; then
                tail -n 30 app.log
              else
                echo "⚠️  Log file 'app.log' not found yet"
                echo "Checking if process is writing to stdout..."
                ps -p $APP_PID -o pid,command
              fi
              echo "=========================================="
              echo ""
            fi

            # On last attempt, show full logs
            if [ $i -eq 30 ]; then
              echo ""
              echo "❌ ERROR: Application failed to start after 150 seconds"
              echo ""
              echo "=========================================="
              echo "📋 FULL APPLICATION LOG:"
              echo "=========================================="
              cat app.log 2>/dev/null || echo "No log file found"
              echo "=========================================="
              echo ""
              echo "Process status:"
              ps -p $APP_PID -o pid,ppid,stat,command || echo "Process not found"
              exit 1
            fi

            sleep 5
          done

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Setup tunnel service
        run: |
          if [ ! -z "${{ secrets.NGROK_AUTH_TOKEN }}" ]; then
            echo "Using ngrok (authenticated)..."
            curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
            echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
            sudo apt update && sudo apt install ngrok
            ngrok config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
            echo "TUNNEL_TYPE=ngrok" >> $GITHUB_ENV
          else
            echo "Using localhost.run (no auth required)..."
            echo "TUNNEL_TYPE=localhost.run" >> $GITHUB_ENV
          fi

      - name: Start tunnel
        run: |
          if [ "$TUNNEL_TYPE" = "ngrok" ]; then
            echo "Starting ngrok tunnel..."
            ngrok http 8080 --log=stdout > tunnel.log 2>&1 &
            TUNNEL_PID=$!
            echo "TUNNEL_PID=$TUNNEL_PID" >> $GITHUB_ENV

            echo "Waiting for ngrok to start..."
            sleep 8

            # Try to get URL from ngrok API
            echo "Fetching tunnel URL from ngrok API..."
            TUNNEL_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url' 2>/dev/null)

            # If that fails, try alternative method
            if [ -z "$TUNNEL_URL" ] || [ "$TUNNEL_URL" = "null" ]; then
              echo "Trying alternative method..."
              TUNNEL_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[] | select(.proto=="https") | .public_url' 2>/dev/null | head -1)
            fi

            # Debug: show what ngrok API returns
            if [ -z "$TUNNEL_URL" ] || [ "$TUNNEL_URL" = "null" ]; then
              echo "⚠️  Could not get URL from ngrok API. Response:"
              curl -s http://localhost:4040/api/tunnels | jq . || echo "API not responding"
              echo ""
              echo "Tunnel log:"
              tail -20 tunnel.log
              TUNNEL_URL="ERROR: ngrok tunnel failed to start"
            fi

          else
            echo "Starting localhost.run tunnel..."
            # Use localhost.run (SSH-based tunnel, no signup needed)
            ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -R 80:localhost:8080 nokey@localhost.run > tunnel.log 2>&1 &
            TUNNEL_PID=$!
            echo "TUNNEL_PID=$TUNNEL_PID" >> $GITHUB_ENV

            echo "Waiting for localhost.run tunnel to establish..."
            sleep 15

            echo "Tunnel log contents:"
            cat tunnel.log
            echo ""

            # Try multiple patterns to extract URL
            TUNNEL_URL=$(grep -oE 'https://[a-zA-Z0-9-]+\.lhr\.life' tunnel.log | head -1)

            if [ -z "$TUNNEL_URL" ]; then
              TUNNEL_URL=$(grep -oE 'https://[a-zA-Z0-9-]+\.localhost\.run' tunnel.log | head -1)
            fi

            if [ -z "$TUNNEL_URL" ]; then
              TUNNEL_URL=$(grep -oE 'Connect to https://[^\s]+' tunnel.log | sed 's/Connect to //' | head -1)
            fi

            if [ -z "$TUNNEL_URL" ]; then
              TUNNEL_URL=$(grep -oE 'http[s]?://[a-zA-Z0-9.-]+' tunnel.log | grep -v 'localhost' | head -1)
            fi

            # Fallback: show what we got
            if [ -z "$TUNNEL_URL" ]; then
              echo "⚠️  Could not extract URL automatically."
              echo "Please check the tunnel log above for the URL."
              TUNNEL_URL="ERROR: Could not extract URL - check logs above"
            fi
          fi

          echo "TUNNEL_URL=$TUNNEL_URL" >> $GITHUB_ENV
          echo "tunnel_url=$TUNNEL_URL" >> $GITHUB_OUTPUT
          echo ""
          echo "=========================================="
          echo "🌐 PUBLIC URL: $TUNNEL_URL"
          echo "=========================================="
        id: tunnel

      - name: Display application info
        run: |
          echo "==================================="
          echo "🚀 Application is running!"
          echo "==================================="
          echo "Public URL: ${{ env.TUNNEL_URL }}"
          echo "Duration: ${{ github.event.inputs.duration_minutes || '60' }} minutes"
          echo "Started at: $(date)"
          echo "Will stop at: $(date -d '+${{ github.event.inputs.duration_minutes || '60' }} minutes')"
          echo "==================================="
          echo ""
          echo "📡 API Endpoints:"
          echo "  - Health: ${{ env.TUNNEL_URL }}/actuator/health"
          echo "  - Rates: ${{ env.TUNNEL_URL }}/api/rates"
          echo "  - Notifications: ${{ env.TUNNEL_URL }}/api/notifications"
          echo "  - Templates: ${{ env.TUNNEL_URL }}/api/templates"
          echo "==================================="
          echo ""
          echo "💡 Example API Requests:"
          echo "  curl ${{ env.TUNNEL_URL }}/api/rates"
          echo "  curl ${{ env.TUNNEL_URL }}/api/rates/EUR"
          echo "==================================="

          # Add to GitHub Actions Summary
          echo "## 🚀 Application Running" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🌐 Public URL" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**${{ env.TUNNEL_URL }}**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📡 API Endpoints" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Health: [${{ env.TUNNEL_URL }}/actuator/health](${{ env.TUNNEL_URL }}/actuator/health)" >> $GITHUB_STEP_SUMMARY
          echo "- Rates: [${{ env.TUNNEL_URL }}/api/rates](${{ env.TUNNEL_URL }}/api/rates)" >> $GITHUB_STEP_SUMMARY
          echo "- Notifications: [${{ env.TUNNEL_URL }}/api/notifications](${{ env.TUNNEL_URL }}/api/notifications)" >> $GITHUB_STEP_SUMMARY
          echo "- Templates: [${{ env.TUNNEL_URL }}/api/templates](${{ env.TUNNEL_URL }}/api/templates)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ⏱️ Runtime" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Duration: ${{ github.event.inputs.duration_minutes || '60' }} minutes" >> $GITHUB_STEP_SUMMARY
          echo "- Started: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "- Will stop: $(date -d '+${{ github.event.inputs.duration_minutes || '60' }} minutes')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 💡 Example cURL Commands" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# Get exchange rates" >> $GITHUB_STEP_SUMMARY
          echo "curl ${{ env.TUNNEL_URL }}/api/rates" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Get specific rate" >> $GITHUB_STEP_SUMMARY
          echo "curl ${{ env.TUNNEL_URL }}/api/rates/EUR" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Create notification" >> $GITHUB_STEP_SUMMARY
          echo 'curl -X POST ${{ env.TUNNEL_URL }}/api/notifications \' >> $GITHUB_STEP_SUMMARY
          echo '  -H "Content-Type: application/json" \' >> $GITHUB_STEP_SUMMARY
          echo '  -d '"'"'[{"userId":1,"type":"EMAIL","templateKey":"welcome","userName":"John","recipientEmail":"john@example.com","sendAt":"2025-11-03T12:00:00","data":{"message":"Hello!"}}]'"'"'' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Create GitHub Summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          # 🚀 Application Running Successfully!

          ## 🌐 Public URL
          **Your application is accessible at:**
          \`\`\`
          ${{ env.TUNNEL_URL }}
          \`\`\`

          ## ⏱️ Runtime Information
          - **Duration:** ${{ github.event.inputs.duration_minutes || '60' }} minutes
          - **Started at:** $(date)
          - **Will stop at:** $(date -d '+${{ github.event.inputs.duration_minutes || '60' }} minutes')
          - **Tunnel Type:** ${{ env.TUNNEL_TYPE }}

          ## 📡 Available API Endpoints

          | Endpoint | URL |
          |----------|-----|
          | Health Check | [\`GET ${{ env.TUNNEL_URL }}/actuator/health\`](${{ env.TUNNEL_URL }}/actuator/health) |
          | Get All Rates | [\`GET ${{ env.TUNNEL_URL }}/api/rates\`](${{ env.TUNNEL_URL }}/api/rates) |
          | Get Rate by Currency | [\`GET ${{ env.TUNNEL_URL }}/api/rates/{currency}\`](${{ env.TUNNEL_URL }}/api/rates/EUR) |
          | Create Notification | [\`POST ${{ env.TUNNEL_URL }}/api/notifications\`](${{ env.TUNNEL_URL }}/api/notifications) |
          | Get Notification | [\`GET ${{ env.TUNNEL_URL }}/api/notifications/{id}\`](${{ env.TUNNEL_URL }}/api/notifications/1) |
          | List Notifications | [\`GET ${{ env.TUNNEL_URL }}/api/notifications\`](${{ env.TUNNEL_URL }}/api/notifications) |
          | Get All Templates | [\`GET ${{ env.TUNNEL_URL }}/api/templates\`](${{ env.TUNNEL_URL }}/api/templates) |

          ## 💡 Example cURL Commands

          ### Get Exchange Rates
          \`\`\`bash
          curl ${{ env.TUNNEL_URL }}/api/rates
          \`\`\`

          ### Get Specific Currency Rate
          \`\`\`bash
          curl ${{ env.TUNNEL_URL }}/api/rates/EUR
          \`\`\`

          ### Create a Notification Job
          \`\`\`bash
          curl -X POST ${{ env.TUNNEL_URL }}/api/notifications \\
            -H "Content-Type: application/json" \\
            -d '{
              "userId": 1,
              "type": "EMAIL",
              "templateKey": "welcome",
              "sendAt": "$(date -u -d '+5 minutes' +%Y-%m-%dT%H:%M:%S)",
              "data": {
                "userName": "John Doe",
                "orderId": "12345"
              }
            }'
          \`\`\`

          ### List All Notifications
          \`\`\`bash
          curl ${{ env.TUNNEL_URL }}/api/notifications
          \`\`\`

          ### Get Templates
          \`\`\`bash
          curl ${{ env.TUNNEL_URL }}/api/templates
          \`\`\`

          ---

          **Note:** The application will automatically stop after ${{ github.event.inputs.duration_minutes || '60' }} minutes.
          EOF

      - name: Run application for specified duration
        run: |
          DURATION_MINUTES=${{ github.event.inputs.duration_minutes || '60' }}
          DURATION_SECONDS=$((DURATION_MINUTES * 60))
          echo "Running application for $DURATION_MINUTES minutes ($DURATION_SECONDS seconds)..."
          echo ""
          echo "🌐 Your application is now accessible at: ${{ env.TUNNEL_URL }}"
          echo ""
          echo "=========================================="
          echo "📋 SPRING BOOT APPLICATION LOGS"
          echo "=========================================="
          echo ""

          # Monitor the application while it runs
          END_TIME=$(($(date +%s) + DURATION_SECONDS))
          LAST_LOG_LINE=0

          while [ $(date +%s) -lt $END_TIME ]; do
            REMAINING=$((END_TIME - $(date +%s)))
            REMAINING_MINUTES=$((REMAINING / 60))
            REMAINING_SECONDS=$((REMAINING % 60))

            # Check if app is still alive
            if ! ps -p $APP_PID > /dev/null; then
              echo ""
              echo "❌ ERROR: Application process died!"
              echo ""
              echo "=========================================="
              echo "📋 LAST 100 LINES OF APPLICATION LOG:"
              echo "=========================================="
              tail -n 100 app.log
              exit 1
            fi

            # Check if tunnel is still alive
            if ! ps -p $TUNNEL_PID > /dev/null 2>&1; then
              echo "⚠️  WARNING: Tunnel died! Attempting to restart..."
              if [ "$TUNNEL_TYPE" = "ngrok" ]; then
                ngrok http 8080 --log=stdout > tunnel.log &
                TUNNEL_PID=$!
                sleep 3
              else
                ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -R 80:localhost:8080 nokey@localhost.run > tunnel.log 2>&1 &
                TUNNEL_PID=$!
                sleep 5
              fi
              echo "TUNNEL_PID=$TUNNEL_PID" >> $GITHUB_ENV
            fi

            # Display status and new logs
            echo ""
            echo "=========================================="
            echo "⏱️  Time remaining: ${REMAINING_MINUTES}m ${REMAINING_SECONDS}s"
            echo ""
            echo "🌐 PUBLIC URL (copy this):"
            echo "   ${{ env.TUNNEL_URL }}"
            echo ""
            echo "📡 Quick test:"
            echo "   curl ${{ env.TUNNEL_URL }}/actuator/health"
            echo "=========================================="

            # Show NEW log lines since last check
            if [ -f app.log ]; then
              CURRENT_LINES=$(wc -l < app.log 2>/dev/null || echo "0")
              if [ $CURRENT_LINES -gt $LAST_LOG_LINE ]; then
                NEW_LINES=$((CURRENT_LINES - LAST_LOG_LINE))
                echo "📋 New application logs (last $NEW_LINES lines):"
                echo "------------------------------------------"
                tail -n $NEW_LINES app.log
                echo "------------------------------------------"
                LAST_LOG_LINE=$CURRENT_LINES
              else
                echo "📋 No new logs in the last minute"
                echo "(Application is idle or logs are not being written)"
              fi
            else
              echo "⚠️  app.log file not found"
            fi
            echo ""

            # Sleep for 1 minute between status updates
            sleep 60
          done

          echo "✅ Duration completed!"

      - name: Show final application logs
        if: always()
        run: |
          echo ""
          echo "=========================================="
          echo "📋 FINAL APPLICATION LOGS (Last 200 lines)"
          echo "=========================================="
          if [ -f app.log ]; then
            tail -n 200 app.log
          else
            echo "No application logs found"
          fi
          echo "=========================================="
          echo ""
          echo "📊 Log Statistics:"
          if [ -f app.log ]; then
            echo "Total log lines: $(wc -l < app.log)"
            echo "Log file size: $(du -h app.log | cut -f1)"
            echo ""
            echo "Error count: $(grep -i "error" app.log | wc -l)"
            echo "Warning count: $(grep -i "warn" app.log | wc -l)"
            echo "Exception count: $(grep -i "exception" app.log | wc -l)"
            echo ""
            echo "Recent errors (if any):"
            grep -i "error\|exception" app.log | tail -n 10 || echo "No errors found"
          fi
          echo "=========================================="

      - name: Stop tunnel
        if: always()
        run: |
          if [ ! -z "$TUNNEL_PID" ] && ps -p $TUNNEL_PID > /dev/null 2>&1; then
            echo "Stopping tunnel..."
            kill $TUNNEL_PID 2>/dev/null || true
          fi

      - name: Stop application
        if: always()
        run: |
          if [ ! -z "$APP_PID" ] && ps -p $APP_PID > /dev/null; then
            echo "Stopping application (PID: $APP_PID)..."
            kill $APP_PID
            sleep 5
            # Force kill if still running
            if ps -p $APP_PID > /dev/null; then
              kill -9 $APP_PID
            fi
            echo "Application stopped."
          else
            echo "Application already stopped."
          fi

